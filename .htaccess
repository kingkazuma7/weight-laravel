<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all requests to public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteCond %{REQUEST_URI} !^/weight-laravel/public/
    RewriteRule ^(.*)$ /weight-laravel/public/$1 [L,QSA]
</IfModule>
